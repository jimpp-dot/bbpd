syntax = "proto3";
    
package grab.mic.light;

option go_package = "../app/pb";

import "broadcast_extension.proto";


message GrabMicLight {
    option (pb.register_name) = 'grab.mic.light';
    option (pb.register_id) = 35;
    repeated uint32 pb = 1;
}