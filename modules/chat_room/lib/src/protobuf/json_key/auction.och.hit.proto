syntax = "proto3";

package auction.och.hit;

option go_package = "../app/pb";

import "broadcast_extension.proto";


message AuctionOchHit {
  option (pb.register_name) = 'auction.och.hit';
  option (pb.register_id) = 113;
  repeated uint32 pb = 1;
}