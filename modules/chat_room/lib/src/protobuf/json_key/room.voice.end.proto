syntax = "proto3";
    
package room.voice.end;

option go_package = "../app/pb";

import "broadcast_extension.proto";


message RoomVoiceEnd {
    option (pb.register_name) = 'room.voice.end';
    option (pb.register_id) = 92;
    uint32 to_rid = 1;
}