syntax = "proto3";
    
package room.vip.mic.sync;

option go_package = "../app/pb";

import "broadcast_extension.proto";

message RoomVipMicSync {
    option (pb.register_name) = 'room.vip.mic.sync';
    option (pb.register_id) = 117;
    repeated uint32 pb = 1;
}