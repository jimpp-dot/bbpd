syntax = "proto3";
    
package auction.sync;

option go_package = "../app/pb";

import "broadcast_extension.proto";


message AuctionSync {
    option (pb.register_name) = 'auction.sync';
    option (pb.register_id) = 5;
    repeated uint32 pb = 1;
}