syntax = "proto3";

package cross.pk.invite;

option go_package = "../app/pb";

import "broadcast_extension.proto";


message CrossPkInvite {
  option (pb.register_name) = 'cross.pk.invite';
  option (pb.register_id) = 21;
  repeated uint32 pb = 1;
}