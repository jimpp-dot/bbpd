name: base_attr_report
description: 归因上报 SDK
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  # connectivity: ^3.0.4
  connectivity_plus: ">=2.3.0 <4.0.0"
  shared_preferences: ^2.0.0
  crypto: ^3.0.3
  convert: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2
